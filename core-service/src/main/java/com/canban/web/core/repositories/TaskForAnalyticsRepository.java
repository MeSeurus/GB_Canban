package com.canban.web.core.repositories;

import com.canban.web.core.entities.TaskForAnalytics;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TaskForAnalyticsRepository extends JpaRepository<TaskForAnalytics, Long> {
}
